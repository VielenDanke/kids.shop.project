server.port=${port:0}

app.elasticsearch.hostAndPort=override
app.elasticsearch.username=elastic
app.elasticsearch.password=override
app.elasticsearch.jks-password=override
app.elasticsearch.jks-store-path=override

# Application configuration
spring.application.name=user-ms

auth.token.key=accessToken
auth.roles.key=roles
user.claims.key=user

spring.rabbitmq.host=localhost
spring.rabbitmq.port=30672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Jwt configs
app.jwt.secret=KASdhwuhdaidj289174918301jd,sadji`<,dKAjsd)I~)
app.jwt.expiration=86400

# Eureka client configuration
eureka.client.service-url.defaultZone=override
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
eureka.instance.healthcheck.enabled=true

spring.cloud.loadbalancer.ribbon.enabled=false

app.url.reserve_cart=http://cloth-ms/clothes/reserve
app.url.decline_cart=http://cloth-ms/clothes/reserve/decline
server.port=${port:51197}

app.elasticsearch.hostAndPort=localhost:9200
app.elasticsearch.username=elastic
app.elasticsearch.password=12345QwErT$
app.elasticsearch.jks-password=elk123$
app.elasticsearch.jks-store-path=${jks.store.path}

# Application configuration
spring.application.name=user-ms

# Eureka client configuration
eureka.client.service-url.defaultZone=http://localhost:8010/eureka
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
